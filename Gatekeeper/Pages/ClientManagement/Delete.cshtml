@page "{id}"
@model DeleteModel
@{
    ViewData["Title"] = "Delete Client";
}

<partial name="_OAuthWarningPartial" />

<div class="row">
    <div class="col-4">
        <div class="card">
            <div class="card-header bg-danger text-white">
                @ViewData["Title"]
            </div>
            <divl class="card-body">
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(Model => Model.ClientToDelete.ClientId)
                    </dt>
                    <dd>
                        @Html.DisplayFor(Model => Model.ClientToDelete.ClientId)
                    </dd>
                    <dt>
                        @Html.DisplayNameFor(Model => Model.ClientToDelete.ClientName)
                    </dt>
                    <dd>
                        @Html.DisplayFor(Model => Model.ClientToDelete.ClientName)
                    </dd>
                    <dt>
                        @Html.DisplayNameFor(Model => Model.ClientToDelete.Description)
                    </dt>
                    <dd>
                        @Html.DisplayFor(Model => Model.ClientToDelete.Description)
                    </dd>
                </dl>
                <form id="delete-form" method="post">
                    <input type="hidden" asp-for="ClientToDelete.Id" />
                    <input type="submit" value="Delete" class="btn btn-danger" />
                    <a asp-page="Index" class="btn btn-primary">Cancel</a>
                </form>
            </divl>
        </div>
    </div>
</div>